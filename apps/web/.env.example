# ─── App ─────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME="2Care"
NEXT_PUBLIC_APP_URL="http://localhost:4200"

# ─── API ─────────────────────────────────────────────────────────────────────
# Server-only: used in Server Components and Route Handlers to call the NestJS
# backend. Never exposed to the browser.
API_URL="http://localhost:3000"

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Must match JWT_SECRET and JWT_REFRESH_SECRET in apps/api/.env exactly.
# Used by the Next.js middleware to validate / decode tokens server-side.
JWT_SECRET="your-jwt-secret"
JWT_REFRESH_SECRET="your-jwt-refresh-secret"
