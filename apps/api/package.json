{
  "name": "@2care/api",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "echo build api",
    "dev": "echo dev api && sleep 99999",
    "lint": "eslint src --ext .ts,.tsx,.js,.jsx --no-error-on-unmatched-pattern",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(api tests)\""
  },
  "dependencies": {
    "@2care/shared": "workspace:*",
    "@2care/schemas": "workspace:*",
    "@2care/types": "workspace:*"
  }
}
